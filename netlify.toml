[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# 让前端可以统一只请求 /api/chat，屏蔽两家平台的函数路径差异
[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
  force = true
